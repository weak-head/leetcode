import pytest
from leetcode.{src} import (
    {method},
)

test_cases = [
#   (args, expectation)
    (None, None),
]

@pytest.mark.timeout(1)
@pytest.mark.parametrize(('args', 'expectation'), test_cases)
def test_{method}(args, expectation):
    got = solve(args)
    assert got == expectation
