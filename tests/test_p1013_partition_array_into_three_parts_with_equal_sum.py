import pytest
from leetcode.p1013_partition_array_into_three_parts_with_equal_sum import canThreePartsEqualSum


@pytest.mark.parametrize(('a', 'expectation'), (
))
def test_(a, expectation):
    assert canThreePartsEqualSum(a) == expectation
