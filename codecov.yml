codecov:
  notify:
    require_ci_to_pass: yes

coverage:
  precision: 2
  round: down
  range: "20...100"

  status:
    project: yes
    patch: no
    changes: no

comment:
  layout: "reach, diff, flags, files, footer"
  behavior: default
  require_changes: no